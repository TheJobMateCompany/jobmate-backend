# ─── Core ──────────────────────────────────────────────────────
# Redis client (Pub/Sub consumer)
redis==5.0.8

# PostgreSQL async driver
asyncpg==0.29.0

# HTTP server (health endpoint)
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ─── NLP ───────────────────────────────────────────────────────
# spaCy for semantic analysis and keyword extraction
# Model downloaded separately: python -m spacy download en_core_web_sm
spacy==3.7.6

# ─── LLM ───────────────────────────────────────────────────────
# OpenAI SDK — used with OpenRouter (OpenAI-compatible API)
# Provider: OpenRouter  →  https://openrouter.ai
# Model:    meta-llama/llama-3.3-70b-instruct
# Config:   base_url=https://openrouter.ai/api/v1  api_key=$OPENROUTER_API_KEY
openai==1.65.4
# httpx: pin to <0.28 to avoid the `proxies` kwarg removal that breaks openai<1.57
# (openai 1.65 supports httpx 0.27.x and 0.28.x transparently)
httpx==0.27.2

# ─── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.9.2
python-json-logger==2.0.7

# ─── Dev / Testing ──────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0

# ─── CV Parsing ─────────────────────────────────────────────────
# PDF text extraction (pure-Python, no native deps)
pdfminer.six==20231228

